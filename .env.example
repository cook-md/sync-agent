# Cook Sync Agent Environment Variables

# Base endpoint for Cook.md
# The sync agent will use this for both web UI (OAuth) and API calls
# For production (default):
# COOK_ENDPOINT=https://cook.md
#
# For local development with Rails on localhost:3000:
# COOK_ENDPOINT=http://localhost:3000

# Rust logging configuration
# Available levels: error, warn, info, debug, trace
# RUST_LOG=cook_sync=info

# Example development configuration:
# COOK_ENDPOINT=http://localhost:3000
# RUST_LOG=cook_sync=debug,info