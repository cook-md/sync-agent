name: Release Please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
      version: ${{ steps.release.outputs.version }}
      major: ${{ steps.release.outputs.major }}
      minor: ${{ steps.release.outputs.minor }}
      patch: ${{ steps.release.outputs.patch }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          token: ${{ secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  build-packages:
    name: Build ${{ matrix.platform.name }}
    needs: release-please
    if: ${{ needs.release-please.outputs.release_created }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - name: Linux x86_64
            os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact_name: linux-x86_64

          - name: macOS x86_64
            os: macos-13
            target: x86_64-apple-darwin
            artifact_name: macos-x86_64

          - name: macOS ARM64
            os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: macos-aarch64

          - name: Windows x86_64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: windows-x86_64

    runs-on: ${{ matrix.platform.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync version to Packager.toml
        shell: bash
        run: |
          VERSION=$(grep '^version = ' Cargo.toml | head -1 | cut -d'"' -f2)
          echo "Syncing version $VERSION to Packager.toml"
          perl -i -pe "s/^version = .*/version = \"$VERSION\"/" Packager.toml

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ matrix.platform.os }}
          cache-on-failure: true

      - name: Cache cargo-packager
        id: cache-cargo-packager
        uses: actions/cache@v4
        with:
          path: ~/.cargo/bin/cargo-packager
          key: ${{ runner.os }}-cargo-packager-0.11.7
          restore-keys: |
            ${{ runner.os }}-cargo-packager-

      - name: Install cargo-packager
        run: |
          if [ "${{ steps.cache-cargo-packager.outputs.cache-hit }}" != "true" ]; then
            echo "Installing cargo-packager..."
            cargo install cargo-packager --locked --force
          else
            echo "Using cached cargo-packager"
            cargo-packager --version
          fi
        shell: bash

      - name: Install Linux dependencies
        if: matrix.platform.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev libappindicator3-dev librsvg2-dev patchelf libxdo-dev

      - name: Setup NSIS (Windows)
        if: matrix.platform.os == 'windows-latest'
        shell: pwsh
        run: |
          # NSIS is pre-installed on GitHub Windows runners
          # Add it to PATH if not already there
          $nsisPath = "C:\Program Files (x86)\NSIS"
          if (Test-Path $nsisPath) {
            echo "$nsisPath" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          }

      - name: Determine package format
        id: format
        run: |
          if [ "${{ matrix.platform.os }}" = "ubuntu-22.04" ]; then
            echo "format=appimage" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.platform.os }}" = "macos-latest" ]; then
            echo "format=dmg" >> $GITHUB_OUTPUT
          elif [ "${{ matrix.platform.os }}" = "windows-latest" ]; then
            echo "format=nsis" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Build release binary
        run: cargo build --release

      - name: Import Code Signing Certificate (macOS)
        if: runner.os == 'macOS'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          # Skip if certificate not set
          if [ -z "$APPLE_CERTIFICATE" ]; then
            echo "⚠️ APPLE_CERTIFICATE not set - skipping code signing"
            exit 0
          fi

          # Decode certificate
          echo $APPLE_CERTIFICATE | base64 --decode > certificate.p12

          # Create and configure keychain
          security create-keychain -p actions temp.keychain
          security list-keychains -d user -s temp.keychain login.keychain
          security default-keychain -s temp.keychain
          security unlock-keychain -p actions temp.keychain
          security set-keychain-settings -lut 21600 temp.keychain

          # Import certificate (-A allows all applications to access)
          security import certificate.p12 -k temp.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -A

          # Allow codesign to access the keychain
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k actions temp.keychain

          echo "✅ Certificate imported successfully"
          rm certificate.p12

      - name: Setup notarization profile (macOS)
        if: runner.os == 'macOS'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          # Skip if certificate not set
          if [ -z "$APPLE_CERTIFICATE" ]; then
            echo "⚠️ Skipping notarization setup - APPLE_CERTIFICATE not set"
            exit 0
          fi

          echo "Creating notarization profile for cargo-packager..."
          xcrun notarytool store-credentials "cook-sync-ci" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_PASSWORD" \
            --team-id "$APPLE_TEAM_ID"

          echo "✅ Notarization profile 'cook-sync-ci' created"

      - name: Package application
        run: cargo packager --release --formats ${{ steps.format.outputs.format }} --verbose
        env:
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_KEYCHAIN_PROFILE: cook-sync-ci
          CARGO_PACKAGER_SIGN_PRIVATE_KEY: ${{ secrets.CARGO_PACKAGER_SIGN_PRIVATE_KEY }}
          CARGO_PACKAGER_SIGN_PRIVATE_KEY_PASSWORD: ${{ secrets.CARGO_PACKAGER_SIGN_PRIVATE_KEY_PASSWORD }}
          CARGO_PACKAGER_PUBLIC_KEY: ${{ secrets.CARGO_PACKAGER_PUBLIC_KEY }}
          # GTK version for linuxdeploy plugin (Linux only)
          DEPLOY_GTK_VERSION: 3

      - name: Cleanup Keychain (macOS)
        if: always() && runner.os == 'macOS'
        run: |
          security delete-keychain temp.keychain || true

      - name: Prepare release assets
        shell: bash
        run: |
          mkdir -p release-assets

          echo "=== Looking for package files ==="
          FOUND=0

          # cargo-packager creates packages in project root, search for them
          for file in $(find . -maxdepth 1 -type f \( -name "*.dmg" -o -name "*.AppImage" -o -name "*.exe" -o -name "*.msi" -o -name "*.sig" \) 2>/dev/null); do
            echo "Found: $file"
            cp -v "$file" release-assets/
            FOUND=1
          done

          if [ $FOUND -eq 0 ]; then
            echo "❌ ERROR: No package files found in project root"
            echo "Files in current directory:"
            ls -lah . | head -20 || true
            exit 1
          fi

          echo ""
          echo "=== Release assets prepared ==="
          ls -lh release-assets/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: packages-${{ matrix.platform.artifact_name }}
          path: release-assets/*
          if-no-files-found: error

  generate-manifest:
    name: Generate Update Manifest
    needs: [release-please, build-packages]
    if: ${{ needs.release-please.outputs.release_created }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Generate manifest.json
        run: |
          VERSION="${{ needs.release-please.outputs.version }}"
          echo "Version: $VERSION"
          ./scripts/generate-manifest.sh "$VERSION" artifacts

      - name: Upload manifest
        uses: actions/upload-artifact@v4
        with:
          name: manifest
          path: manifest.json
          if-no-files-found: error

  publish-release:
    name: Publish GitHub Release
    needs: [release-please, build-packages, generate-manifest]
    if: ${{ needs.release-please.outputs.release_created }}
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release-assets
          # Copy all package files (now in root of each artifact directory)
          find artifacts -type f \( -name "*.dmg" -o -name "*.AppImage" -o -name "*.exe" -o -name "*.msi" -o -name "*.sig" \) -exec cp {} release-assets/ \;
          # Copy manifest
          cp artifacts/manifest/manifest.json release-assets/
          echo "Release assets prepared:"
          ls -lh release-assets/

      - name: Upload assets to existing release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.release-please.outputs.tag_name }}
          files: release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
